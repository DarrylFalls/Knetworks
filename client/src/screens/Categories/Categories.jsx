

const Categories = ({categories}) => {
  return (
    <div>
      <div>Categories</div>
      {
      categories.map((category) => (
        <div>
          
        </div>
      ))}
    </div>
  )
}

export default Categories